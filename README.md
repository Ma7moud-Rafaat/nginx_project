# nginx_project
This project packages a fully responsive Bootstrap Agency Template into a Docker container and serves it using Nginx. The container delivers a fast, production-ready static website accessible on
